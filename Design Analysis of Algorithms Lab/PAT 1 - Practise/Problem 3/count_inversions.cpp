// Consider an array of A[1, 2, 3, ..., n] to be an array of n distinct numbers.
// If i < j and A[i] > A[j], then we call the the pair (i, j) as an inversion of A. For example, the
// five inversions in the array A :< 2, 3, 8, 6, 1 > are (1, 5), (2, 5), (3, 4), (3, 5), (4, 5). Given an
// array of numbers, design the pseudocode and the corresponding code that follows
// ‘Divide-Conquer-Combine’ strategy for computing the number of inversions in the array
// and analyse the same.

